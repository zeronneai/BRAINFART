# ── Client (exposed to browser, safe: anon key only) ─────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── Serverless only (NEVER exposed client-side) ──────────────────────
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — real nearby-business lookup (Phase 2; mock El Paso data used when unset)
GOOGLE_PLACES_API_KEY=
